(defpackage advent-of-code
  (:use :cl :arrow-macros))

(in-package :advent-of-code)

;; (defun add-day (number)
;;   (let* ((dir-pathname (make-pathname number))
;;          (input-dir (get-input-dir-pathname))
;;          (package-info '(defpackage )))))
